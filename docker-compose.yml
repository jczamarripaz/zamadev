services:
  angular-app:
    build:
      context: .
      args:
        - ENV_CONFIGURATION=${ENV_NAME:-dev} 
    container_name: ${CONTAINER_NAME:-zamadev-dev}
    expose:
      - "80"
    networks:
      - proxy-network
    restart: always

networks:
  proxy-network:
    external: true